import CreateAdButton from "../CreateAdButton";
import SearchForm from "../SearchForm";

function Header() {
  return (
    <header>
      <SearchForm />
      <CreateAdButton />
    </header>
  );
}

export default Header;
