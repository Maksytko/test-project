import Section from "../Section";
import AdsList from "../AdsList";

function Main() {
  return (
    <main>
      <Section>
        <AdsList />
      </Section>
    </main>
  );
}

export default Main;
